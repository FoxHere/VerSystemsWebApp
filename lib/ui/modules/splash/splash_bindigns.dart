import 'package:versystems_app/config/utils/auto_dispose_bindings.dart';

class SplashBindigns extends AutoDisposeBindings {
  @override
  void dependencies() {
    // autoPut(true, tag: 'isInitialized', permanent: true);
  }
}
