import 'package:versystems_app/config/utils/auto_dispose_bindings.dart';

class CompanySwitcherBindings extends AutoDisposeBindings {
  @override
  void dependencies() {
    // Controller and Repositories are already injected globally
  }
}
